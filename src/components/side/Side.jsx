import React from "react";
import styles from "./side.scss";

export default function Side() {
  return (
    <div className={ styles.side }>
      <header className={ styles.side__header }>Workbox</header>
    </div>
  );
}